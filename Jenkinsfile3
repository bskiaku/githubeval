pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                sh '''
                docker build -t my-nginx /var/lib/jenkins/workspace/
                docker images | grep my-nginx
                '''
            }
        }
    }
}
